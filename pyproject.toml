[project]
name = "congressional-world-view"
version = "0.1.0"
description = "Visualize which countries dominate U.S. congressional language over time"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
