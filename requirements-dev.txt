# Exact pins, same rationale as requirements.txt: tests.yml pip-installs
# this file in CI. Versions match the locally verified .venv and all
# resolve on PyPI.
-r requirements.txt
pytest==9.1.1
pytest-cov==7.1.0
responses==0.26.2
freezegun==1.5.5
