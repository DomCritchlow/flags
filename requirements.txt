# Exact pins: CI installs these while holding two API keys and push-to-main
# rights, so an unpinned transitive upgrade is a supply-chain risk.
# Versions match the locally verified .venv and all resolve on PyPI.
pyahocorasick==2.3.1
pyyaml==6.0.3
requests==2.34.2
anthropic==0.121.0
python-dateutil==2.9.0.post0
click==8.4.2
